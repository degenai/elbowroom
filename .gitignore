# Wrangler local state — carries the Cloudflare account id + cache; never commit.
.wrangler/
node_modules/
.dev.vars
*.log
.DS_Store
Thumbs.db

# Bot agent scratchpad memory: keep out of the repo (fixes .Jules/.jules case-collision)
.jules/
.Jules/
